/** Pages Pilotage & Gestion (dashboard, agenda, clients, employés, services, stock, dépenses, paie, rapports, comptes, journal, paramètres) — français → arabe. */
const pagesGestion: Record<string, string> = {};

export default pagesGestion;
